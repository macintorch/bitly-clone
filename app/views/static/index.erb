<h1>Cloning Bitly</h1>

<form action="/urls" method="post">
	Enter URL:
	<input type = "text" name="long_url" placeholder="http://">
	<input type = "submit" value="Submit">
</form>
<br><br>
	<table border="1" style="width:50%" align="center">
    
    <td>No.</td> 
    <td>Original URL</td>
    <td>Shorten URL</td>
  <tr><% @urls.each_with_index do |url, index| %>
    <td><%= index + 1 %></td> 
    <td><%= url.long_url %></td>
    <td><%= url.short_url %></td>

  </tr>
  <% end %>
</table>